/** @type {import('next').NextConfig} */
const nextConfig = {
  // Vercel deployment - no static export (API routes need to work)
  // API routes will be serverless functions on Vercel
};

module.exports = nextConfig;
